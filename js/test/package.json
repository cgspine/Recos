{
  "name": "recos-test",
  "version": "1.0.0",
  "description": "",
  "main": "cli.js",
  "scripts": {
    "build": "recos-cli -c config.js -o dist",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
  },
  "devDependencies": {
    "@babel/types": "^7.14.5"
  }
}
